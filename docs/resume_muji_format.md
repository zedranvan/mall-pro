你的名字 - Java 后端开发工程师 / 交付工程师

::: left

icon:info 男 / 2002.05
icon:phone 138-xxxx-xxxx
icon:email your_email@163.com

:::

::: right

icon:map-pin 广州 / 深圳 / 佛山（随时到岗）
icon:github https://github.com/your_github
icon:award 统招全日制本科（工学学士·学信网双证齐全）

:::

教育背景

::: left

某某大学 - 计算机科学与技术（本科）

:::

::: right

2022.09 - 2024.06

:::

主修课程：数据结构与算法、操作系统、计算机网络、数据库系统概论、软件工程。在校期间系统掌握分布式中间件与服务端研发体系，具备扎实的计算机工程底座。

::: left

某某职业技术学院 - 软件技术（专科）

:::

::: right

2019.09 - 2022.06

:::

主修课程：Java程序设计、Linux操作系统、关系型数据库设计、Web前端技术开发。

工作经历

::: left

广州市位讯信息科技有限责任公司 - 研发部 - Java开发工程师

:::

::: right

2024.03 - 2024.09

:::

- 参与公司智慧文旅票务分销中台与医疗文献检索系统服务端核心链路的设计、重构与性能调优。
- 负责文旅特惠限时抢购链路的防超卖设计与跨系统可靠性交付，推进 Transactional Outbox 事务发件箱模式在出票通知链路中落地。
- 主导核心业务接口的性能压测与生产级瓶颈排查，解决海量文献导出引起的 JVM OOM 堆内存泄露，优化数据库慢查询执行计划。

项目经历

智慧文旅 5A 景区假日票务分销与高并发履约中台

Spring Boot 3.3.4 PostgreSQL 16 Redis 7.2 Kafka 3.7.0 Docker Caffeine

项目描述：
面向大湾区 5A 级景区节假日特惠早鸟票秒杀及多渠道分销的高可用履约系统。核心解决大促峰值流量下的门票配额瞬时防超卖、跨系统（线下闸机、短信通知）事件投递最终一致性，以及门票全生命周期状态机的防乱序控制。

工作内容：

- 门票配额防超卖攻防设计：评估了数据库行排他锁（严重耗尽连接池）与 Redisson 分布式锁（高竞争下网络 RTT 与自旋等待开销大）的弊端，选型为 Redis + Lua 脚本在内存中完成“用户限购校验 + 门票配额原子预扣”，将无效流量拦截在上游；在 PostgreSQL 数据库层保留物理库存非负约束（CHECK stock >= 0）作为终极底线兜底，杜绝门票超发。
- 事务性发件箱模式（Transactional Outbox）落地：为彻底杜绝分布式事务“假一致性”隐患（数据库回滚而消息已发送，或消息丢失下游闸机未出票），在本地数据库设计 t_outbox 事务发件箱表，将订单生成与出票事件绑定在同一个 PostgreSQL 强原子事务中；编写可靠投递引擎，基于 FOR UPDATE SKIP LOCKED 实现多实例无锁竞争的高并发拉取，异步可靠推送到 Kafka，消息投递可靠率达 100%。
- 门票有限状态机（FSM）与乐观锁防乱序：设计严格的状态跃迁矩阵（待支付 -> 已出票 -> 已入园核销 / 已退票），终态严禁非法回退；在 order_info 引入 version 乐观锁版本号，彻底解决因网络抖动导致“退票取消”与“闸机核销”并发到达引发的时序错乱资损。
- 压测剖析与大事务调优：编写 Python 异步高并发测试模型模拟 1000 独立用户并发压测，定位到系统吞吐量瓶颈在于 @Transactional 范围过大导致 HikariCP 连接池等待超时；剥离无状态计算后单节点实测达到 1049.78 QPS，P99 响应延迟缩减 55%，全链路 0 脏数据。

智慧医疗临床文献智能检索与科研数据分发系统

Spring Boot Spring Data JPA Redis Guava Bloom Filter EasyExcel PostgreSQL

项目描述：
面向三甲医院与高校医学院打造的临床医学科研文献、药物试验报告在线检索与数据分发平台。负责百万级文献 DOI 快速索引、爬虫恶意探测防御以及十万级科研文献流式导出。

工作内容：

- 布隆过滤器防穿透多级架构：针对海外爬虫恶意枚举文献 DOI 编码导致数据库持续穿透的问题，选用 Guava Bloom Filter（设定容量 1000 万，容错率 fpp = 0.01%）在网关接入层前置拦截非法请求；针对误判流量在 Redis 补充缓存空对象（短 TTL 60s）二级兜底，将数据库非法探针穿透率压制至 0.01% 以下。
- 高频阅读计数防丢更新重构：定位到早间科研高峰期数据库并发执行 UPDATE 文献阅读数时频繁发生“丢失更新（Lost Update）”及行锁争抢；重构为 Redis INCR 内存高频原子自增 + 定时任务异步批量刷新落库方案，单接口跳转时延稳定在 15ms 内。
- 生产级 OOM 内存泄露排查与流式重构：针对科研人员导出 30 万条跨国病例文献索引时原生 DOM 解析导致 JVM 堆内存 Old 代打满、频繁 Full GC 最终触发 OOM 的严重缺陷，使用 JProfiler 堆转储分析（Heap Dump）定位大对象驻留根因；重构为 EasyExcel 基于 SAX 事件驱动的流式逐行读写，老年代内存占用峰值由 1.8GB 降至 100MB 以内。

专业技能

- Java基础与并发：扎实的 Java 基础与并发编程（JUC），熟练掌握线程池配置、ReentrantLock / synchronized 锁机制与常见设计模式，具备多线程竞态条件排查与死锁防御能力。
- 框架与事务：熟练使用 Spring Boot 3、Spring Data JPA、MyBatis-Plus 等主流框架，深入理解声明式事务传播机制与大事务拆解优化。
- 数据库与调优：熟练掌握 PostgreSQL / MySQL 事务隔离机制（MVCC）；熟练运用 EXPLAIN 分析慢 SQL 执行计划，具备复合索引设计、行级排他锁争抢优化与物理约束兜底设计能力。
- 中间件与分布式：熟练使用 Redis 核心数据结构及 Lua 脚本实现原子并发控制；掌握 Kafka 生产消费机制，具备基于 Transactional Outbox 模式保障分布式最终一致性的实战经验。
- 交付与排错：熟练在 Linux 环境下排查问题（熟练使用 top, jstat, netstat, journalctl），熟练运用 Docker 容器化编排；具备基于 Heap Dump 分析排查生产级 JVM 内存溢出的实战经验。
