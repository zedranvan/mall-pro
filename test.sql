CREATE TABLE IF NOT EXISTS t_brand(
	id		BIGINT GENERATED BY DEFAULT AS INDETITY PRIMARY KEY,
	name 	VARCHAR(64) NOT NULL,
	long_url	VARCAHR(255) NULL,
	description TEXT NULL
);

CREATE TABLE IF NOT EXISTS t_brand(
	id 
	)